<% unless @upcoming_camps.empty? %>
  <h6>&nbsp;</h6>
  <div class="panel clearfix">
    <h5>Upcoming Camps</h5>
    <ul class="side-nav">
      <% @upcoming_camps.limit(7).each do |camp| %>
        <li><%= link_to camp.name, camp_path(camp) %> (<%= camp.start_date.strftime('%m/%d/%y') %>)</li>
      <% end %>
    </ul>
  </div>
<% end %>
